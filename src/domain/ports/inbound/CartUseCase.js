export default class CartUseCase {
    addItemToCart(cartId, item) {
        throw new Error('Método deve ser implementado');
    }

    calculateTotal(cartId, discountType) {
        throw new Error('Método deve ser implementado');
    }
}